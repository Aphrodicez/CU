import numpy as np

def p( X ):
    # X เป็นอาเรย์ขนาด n^2 เก็บจำนวนโจทย์ที่ทำ (คอลัมน์ 0) กับเกรดเฉลี่ย (คอลัมน์ 1) ของนักเรียน n คน
    # คืนอาเรย์ขนาด n ช่อง เก็บความน่าจะเป็นที่นักเรียนแต่ละคนจะเรียนผ่านวิชา คำนวณจากสูตรข้างบน
    # ใชความสามารถของ NumPy จะเขียนได้โดยไม่ต้องใชวังวน (อย่างมาก 3 บรรทัด)
    return 1 / (1 + np.e ** -(-3.98 + 0.1 * X[::, 0] + 0.5 * X[::, 1]))

exec(input().strip()) # ต้องมีคำสั่งนี้ ตรงนี้ตอนส่งให้ Grader ตรวจ